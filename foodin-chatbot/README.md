# FoodIn Chatbot (Phase 1)

## Quick start
1. Create virtualenv and install deps:

2. Place trained artifacts in `data/`:
- `data/vectorizer.pkl`
- `data/chatbot_model.pkl`
- `data/intents.json`

3. Run:

4. Test:


<!-- -for entity extraction we use spacy and regex in the chatbot   spacy is the brain that understand all the label and relationship between entities like gwalior place 4 quantiy of item if 4 pizza then after that regex is like sniper that extract time date am pm info from the sentence  -->